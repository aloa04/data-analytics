'''
Write a program that can tell you if a number (integer) is prime or not.

Prime Number: Those numbers that are only divisible by themselves and 1
They are greater than 1: [''2, 3, 5, 7, 11, 11, 13, 17, 19, 23, etc. ]
'''

def task8Basic():
  num: int = int(input('Enter an integer number: '))

  if num > 1:
      for i in range(2,int(num)):
          if (int(num) % i) == 0:
              print(f"Sorry, the number {num} is not prime. It is divisible by {i}")
              break
      else:
          print(f"Well done, the number {num} is prime!")
  else:
      print(f"Sorry, the number {num} is not prime. Prime numbers are greater than 1.")